import reducer from './reducers'

import { default as pokemonBaseTypes } from './types'
import { default as pokemonBaseActions } from './actions'
import { default as pokemonBaseOperations } from './operations'
import { default as pokemonBaseSelectors } from './selectors'

export {
  pokemonBaseTypes,
  pokemonBaseActions,
  pokemonBaseOperations,
  pokemonBaseSelectors
}

export default reducer
